{
  "name": "hackoss-api",
  "version": "0.0.0",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "clean": "rm -rf dist && mkdir dist",
    "build": "npm run lint && npm run clean && tsc --skipLibCheck",
    "postinstall": "npm run build",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "@types/express": "^4.16.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "hackoss": "^0.2.6",
    "prismic-javascript": "^2.0.1"
  },
  "devDependencies": {
    "typescript": "~3.2.4",
    "@types/node": "~10.12.18",
    "ts-node": "~7.0.1",
    "tslint": "~5.12.1"
  }
}
